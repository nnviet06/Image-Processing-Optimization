// src/gpu/utils.cu
// GPU shared utilities (stub for now)

#include <cuda_runtime.h>

// Placeholder - can expand with CUDA memory helpers later
void gpu_init() {
    // Warm up GPU context
    int device;
    cudaGetDevice(&device);
}